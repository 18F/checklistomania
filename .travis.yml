language: node_js
node_js:
    - 0.12
before_script:
    - "npm i -g jasmine-node"
    - "nohup node app.js --skip-auth &"
    - "sleep 5"
script:
    - "npm test"
env:
    - GITHUB_CLIENT_ID=test GITHUB_CLIENT_SECRET=testSecret SKIP_AUTH=true
services:
  - mongodb
